import React from 'react';
import { View, Text } from 'react-native';

const Modal = () => {
    return (
        <View>
            <Text>Modal Content</Text>
        </View>
    );
};

export default Modal;  // Certifique-se de que a exportação está correta
